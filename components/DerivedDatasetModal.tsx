"use client";

import React from "react";

export default function DerivedDatasetModal({
  onClose,
}: {
  onClose?: () => void;
}) {
  // Placeholder modal — renders nothing until implemented
  return null;
}
